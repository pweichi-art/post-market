# SPEC — 盤後小幫手（台股扣抵值 / 均線上彎分析）

> 版本 v0.1（2026-09-02）｜狀態：需求確認完成，尚未開發
> 專案資料夾：`g:\wc agent\PostMarket\`
> App 暫定名稱：**盤後小幫手**（可再改）

---

## 1. 產品目標

一個**收盤後**使用的台股個股分析工具。輸入股票名稱或代號，就能看到這檔股票的：

- **扣抵值分析**：未來 N 天，股價要站上多少，5/10/20/60 日均線才會「上彎」
- **均線與 K 線圖**
- **三大法人買賣超**（外資 / 投信 / 自營商）
- **融資融券餘額變化**

一句話介紹：**「幫我在收盤後判斷一檔股票的均線何時、在什麼價位會翻上來的小工具。」**

---

## 2. 目標使用者

- **只有作者一人使用**
- 使用時間：台股收盤後（13:30 之後）
- 裝置：Android 手機 + Windows 電腦都要能用
- 不需要註冊、登入、會員系統

---

## 3. 名詞定義（重要，實作依此為準）

### 3.1 均線（MA, Moving Average）
第 N 日均線 = 最近 N 個交易日「收盤價」的平均。
本專案預設追蹤 4 條：**MA5 / MA10 / MA20 / MA60**。

### 3.2 扣抵值（本專案核心）
把收盤價序列記為 `close[]`，今天是第 `t` 天。

- 今天的 `MA(N)` = `close[t-N+1 .. t]` 的平均
- 明天多一根新資料，計算範圍變成 `close[t-N+2 .. t+1]`，**被踢掉的是 `close[t-N+1]`**
- 這個被踢掉的價格就是**明日扣抵值**

**推廣：未來第 k 個交易日的扣抵值（k = 1..N）= `close[t-N+k]`**
（這些都是「已知的歷史價格」，所以未來 N 天的扣抵值現在就能全部算出來）

### 3.3 均線上彎條件
未來第 k 日：**當日收盤價 > 該日扣抵值 → MA(N) 當日往上（上彎）**
反之則下彎；相等則走平。

### 3.4 基準價
= 「當日的扣抵值」。股價站上基準價，均線就會由下彎轉為走平 / 上彎。
畫面用語統一叫「**均線上彎所需價位**」。

### 3.5 每日均線變化量（估計）
假設股價固定在 `P`，則 `MA(N)` 每天變化 ≈ `(P - 當日扣抵值) / N`。
用來估「若股價維持在現價，均線幾天後翻上」。

---

## 4. 功能範圍

### 4.1 MVP（第一版，唯一目標）
| # | 功能 | 說明 |
|---|---|---|
| M1 | 股票搜尋 | 輸入代號（如 `2330`）或名稱（如 `台積電`）→ 進入個股頁 |
| M2 | 扣抵值分析表 | 顯示 MA5/10/20/60 各自：目前均價、目前是上彎/下彎、未來 10 個交易日的扣抵值與「上彎所需價位」 |
| M3 | 資料抓取 | 打開 App 當下，向免費 API 抓該股近 1 年日收盤價 |
| M4 | 本機快取 | 抓到的資料存進瀏覽器 IndexedDB，下次優先讀快取、只補新資料 |
| M5 | 免責聲明 | 頁尾固定顯示「本工具僅供參考，非投資建議」 |
| M6 | 防呆 | 抓不到 / API 失敗 → 顯示「今日資料尚未更新」，不白屏、不報錯給使用者 |

### 4.2 MVP 之後（依序）
| 階段 | 功能 |
|---|---|
| P1 | K 線圖 + 4 條均線疊圖 |
| P2 | 三大法人近 20 日買賣超（表格 + 長條圖） |
| P3 | 融資融券餘額變化 |
| P4 | 觀察清單（釘選數檔，首頁一次看完各檔「距離上彎還差幾元」） |
| P5 | 全市場掃描：列出「明日均線即將上彎」的候選股 |
| P6 | 卡通風視覺完稿 + PWA 安裝設定（加到主畫面、離線讀快取） |

### 4.3 明確不做
- ❌ 即時報價 / 盤中看盤
- ❌ 自動下單、券商串接
- ❌ 「買進 / 賣出」字樣或買賣訊號燈號（只呈現數據）
- ❌ 多使用者、雲端同步、後端伺服器

---

## 5. 頁面架構

```
首頁 /
 ├─ 搜尋框（代號 / 名稱）
 └─ 觀察清單（P4 之後才有內容；MVP 先留位置）

個股分析頁 /stock/:code
 ├─ 標題列：代號 名稱 收盤價 漲跌
 ├─ 扣抵值分析表（MVP 主角）
 ├─ K 線圖 + 均線（P1）
 ├─ 三大法人（P2）
 ├─ 融資融券（P3）
 └─ 頁尾免責聲明

設定頁 /settings
 ├─ 均線天數自訂（預設 5/10/20/60）
 ├─ 歷史回補天數（預設 250 交易日 ≈ 1 年）
 ├─ 清除快取 / 重新抓取
 └─ 資料來源與版本資訊
```

路由用網址 hash（`#/stock/2330`）即可，免後端。

---

## 6. 使用者流程

```
打開 App
  → 首頁：輸入「2330」或「台積電」
  → 按搜尋
  → App 檢查 IndexedDB 有沒有這檔的快取
       有 → 直接算、直接顯示；背景補抓今天缺的資料
       沒有 → 顯示載入動畫 → 向 API 抓近 1 年資料 → 存快取 → 顯示
  → 個股頁看扣抵值分析表
  → （可選）加入觀察清單
```

---

## 7. 技術架構

| 層 | 選擇 | 理由 |
|---|---|---|
| 型態 | **PWA**（純前端網頁，可加到手機桌面） | 寫一次，Android + 電腦通用，免上架 |
| 語言 | HTML + CSS + 原生 JavaScript（ES Modules） | 新手友善、無建置步驟、GitHub Pages 直接放 |
| 圖表 | `lightweight-charts`（TradingView 開源，免費）走 CDN | K 線 / 均線最省力 |
| 本機儲存 | IndexedDB（用 `idb-keyval` 輕量套件） | 存歷史股價快取 |
| 資料來源 | 主：**FinMind API**（支援瀏覽器跨網域）｜備援：**證交所 OpenAPI** | 免費、免金鑰或免費金鑰 |
| 佈署 | **GitHub Pages** | 免費、順便學 Git |
| 後端 | **無** | 打開當下才抓資料，不需要 |

### 7.1 資料來源細節
- **股票清單（搜尋用）**：FinMind `TaiwanStockInfo` 或證交所 `t187ap03_L`，抓一次存快取。
- **日 K（收盤價）**：FinMind `TaiwanStockPrice`，參數 `data_id`=代號、`start_date`=一年前。
- **三大法人**：FinMind `TaiwanStockInstitutionalInvestorsBuySell`。
- **融資融券**：FinMind `TaiwanStockMarginPurchaseShortSale`。
- FinMind 免登入約 300 次/小時，單人使用足夠；必要時申請免費 token 提高額度。

### 7.2 CORS 風險與對策
若某 API 不允許瀏覽器直接呼叫（CORS 被擋）：
1. 優先換另一個允許的來源；
2. 真的都不行 → 加一個免費的中繼代理（Cloudflare Workers 免費方案），這是唯一可能需要的雲端元件。開發時遇到再處理。

---

## 8. 風險管理設計（依 Q12 確認）

| # | 項目 | 實作方式 |
|---|---|---|
| R1 | 免責聲明 | 每頁頁尾固定：「本工具僅供個人研究參考，非投資建議，據此操作損益自負。」 |
| R2 | 資料失敗防呆 | API 失敗 / 逾時 / 空資料 → 顯示「今日資料尚未更新，請稍後再試」，其餘畫面用最後一次快取顯示並標註資料日期 |
| R3 | 不做買賣訊號 | 全站不出現「買 / 賣 / 進場 / 出場」字樣與紅綠燈號，只呈現數字、方向（上彎 / 下彎）與圖表 |
| R4 | 資料日期透明 | 每個數據區塊標「資料日期：YYYY-MM-DD」，讓使用者知道新舊 |
| R5 | 個人資料 | 全部存在自己裝置，不上傳、不收集，無帳號 |

---

## 9. 驗收標準（MVP Done 的定義）

- [ ] 在手機與電腦瀏覽器都能打開，加到主畫面能全螢幕開啟
- [ ] 輸入 `2330` 和 `台積電` 都能進到台積電個股頁
- [ ] 扣抵值分析表的數字，能用 Excel 手算對照驗證（誤差 = 0）
- [ ] 關掉網路再打開，能顯示上次快取資料 + 「離線中」提示
- [ ] 把 API 網址故意打錯，畫面顯示友善錯誤訊息而不是白屏
- [ ] 頁尾永遠看得到免責聲明

---

## 10. 開發優先順序（依 Q13 確認）

1. **Python 原型**：本機跑通「抓一檔股票資料 → 算扣抵值 → print 表格」，驗證數學正確
2. **網頁 MVP**：搜尋 → 扣抵值分析表（M1–M6）
3. K 線圖 + 均線（P1）
4. 三大法人（P2）
5. 融資融券（P3）
6. 觀察清單（P4）
7. 全市場掃描（P5）
8. 卡通風完稿 + PWA 設定（P6）

### 時程假設
每天至少 1 小時 → **MVP 目標 2 週內完成**。
